version: '3.8'

services:
  # Banco de dados da aplicação (MongoDB)
  debitboard-mongo-db:
    image: mongo:8.0
    container_name: debitboard-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root123
      MONGO_INITDB_DATABASE: sast_platform
    volumes:
      - mongo_data:/data/db
      - ./scripts/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
    networks:
      - debitboard-network

  # Banco de dados do Keycloak (Postgres é mais robusto que H2)
  debitboard-keycloak-db:
    image: postgres:16
    container_name: debitboard-keycloak-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: keycloak
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - debitboard-network

  # Serviço de Identidade (Keycloak)
  debitboard-keycloak:
    image: quay.io/keycloak/keycloak:26.7.2
    container_name: debitboard-keycloak
    command: start-dev
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://debitboard-keycloak-db:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: keycloak
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HOSTNAME: localhost
      KEYCLOAKIFY_SPA_DEV_SERVER_PORT: 5173
    volumes:
      - ./.docker/providers:/opt/keycloak/providers
    depends_on:
      - debitboard-keycloak-db
    networks:
      - debitboard-network

volumes:
  mongo_data:
  postgres_data:

networks:
  debitboard-network:
    driver: bridge